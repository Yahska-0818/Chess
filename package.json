{
  "name": "chess",
  "version": "1.0.0",
  "description": "chess game",
  "homepage": "https://github.com/Yahska-0818/Chess#readme",
  "bugs": {
    "url": "https://github.com/Yahska-0818/Chess/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Yahska-0818/Chess.git"
  },
  "license": "ISC",
  "author": "Akshay",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development node --watch index.js",
    "test": "NODE_ENV=test node --test",
    "lint": "eslint .",
    "build:ui": "rm -rf dist && cd frontend && npm run build && cp -r dist ../",
    "deploy:full": "npm run build:ui && git add . && git commit -m newDeployBuild && git push"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.2",
    "socket.io": "^4.8.1"
  }
}
